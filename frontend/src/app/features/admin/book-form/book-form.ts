import { Component } from '@angular/core';

// Placeholder — implemented in Story 5.3
@Component({
  selector: 'app-book-form',
  template: `<p>Formulaire livre (admin) — à venir</p>`,
})
export class BookForm {}
